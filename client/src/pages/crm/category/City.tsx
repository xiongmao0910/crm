import { CityTable } from "../../../layout";

const City = () => {
    return (
        <div className="section-table flex-1">
            <CityTable />
        </div>
    );
};

export default City;
